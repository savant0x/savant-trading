# FID: Complete System Build — 10 Improvements + Dashboard + Glass House

**ID:** FID-2026-0530-016
**Phase:** FID-START (initial)
**Severity:** critical
**Status:** decomposed
**Created:** 2026-05-30 12:09
**Author:** Kilo (Agent)

---

## Summary

Complete build-out of the Savant Trading system: 10 remaining improvements, a production dashboard UI (matching Savant's design system), and Obsidian vault integration via the Glass House pattern. Includes transaction logs, balance history, AI decision tracking, and full control over the trading engine via the dashboard.

## Environment

- **OS:** Windows 11 (win32)
- **Language/Runtime:** Rust 2021 (engine), Next.js 16 + React 19 + TypeScript 5 (dashboard)
- **Tool Versions:** cargo 1.x, Node.js 22+, npm/yarn
- **Commit/State:** v0.1.2 tag, build + clippy + fmt clean
- **Dashboard Design Reference:** Savant main project — `C:\Users\spenc\dev\Savant\dashboard\`
- **Glass House Reference:** Savant obsidian crate — `C:\Users\spenc\dev\Savant\crates\obsidian\`

---

## Design System (From Savant)

| Token | Value | Usage |
|-------|-------|-------|
| `--accent` | `#00d5ff` | Primary accent, links, active states |
| `--bg` | `#050505` | Page background |
| `--bg-glass` | `rgba(255,255,255,0.03)` | Glass morphism panels |
| `--border` | `rgba(0,213,255,0.1)` | Subtle borders |
| `--text` | `#e0e0e0` | Primary text |
| `--text-dim` | `#666` | Secondary text |
| `--success` | `#00ff88` | Profit, positive |
| `--error` | `#ff4444` | Loss, negative |
| `--warning` | `#ffaa00` | Warnings, caution |
| `--font-mono` | JetBrains Mono | Code, telemetry, prices |
| `--font-body` | Inter | Body text |
| `--glass-blur` | `blur(12px)` | Backdrop filter |
| `--neon-glow` | `0 0 10px rgba(0,213,255,0.3)` | Text shadow on accent |

### Layout

- 3-column CSS grid: left sidebar (280px), main content (1fr), right panel (min(750px, 45vw))
- Collapsible sidebar and right panel
- Glass morphism panels with backdrop-filter
- Custom scrollbars with accent color
- Radial gradient ambient backgrounds

---

## 10 Improvements

### 1. End-to-End Dry-Run Test (CRITICAL)

**What:** `cargo run -- --dry-run` makes ONE AI call and prints the full pipeline.

**Output:**
```
=== SAVANT DRY RUN ===
Pair: BTC/USD
Time: 2026-05-30 12:00:00 UTC

--- MARKET DATA ---
Candle: O=73800 H=74100 L=73500 C=73950 V=1234
Indicators: EMA_FAST=73900 EMA_SLOW=73200 RSI=58 ATR=450 ADX=22 VWAP=73850
Regime: Ranging

--- INSIGHT ---
Fear & Greed: 23 (Extreme Fear)
BTC Dominance: 57.4%
Funding Rate: -0.0012%
OI: 45000 contracts
Block Height: 951727
RSS: 45 items from 15 feeds

--- KNOWLEDGE SELECTION ---
Conditions: [Ranging, ExtremeFear]
Selected 8 of 141 units:
  1. crypto-fcb-001 (Fear & Greed Index) — priority 5
  2. fabio-amt-002 (Mean Reversion Model) — priority 5
  3. crypto-fcb-009 (Liquidation Heatmaps) — priority 5
  ...

--- PROMPT ---
[Full system prompt with knowledge injected]

--- LLM RESPONSE ---
{"action":"HOLD","pair":"BTC/USD",...}

--- PARSED DECISION ---
Action: HOLD
Confidence: 0.45
Reasoning: "Market ranging, Fear at 23 but no clear setup..."
Knowledge Sources: crypto-fcb-001, fabio-amt-002

=== DRY RUN COMPLETE ===
```

**Files:**
- `src/main.rs` — Add `--dry-run` flag
- `src/engine.rs` — Add `dry_run()` function

### 2. External Knowledge Loading

**What:** Load knowledge from `knowledge/` directory at project root, not compiled into binary.

**Behavior:**
- If `knowledge/` dir exists → load JSON files from it
- If not found → fall back to embedded (compiled-in) knowledge
- Configurable via `[ai].knowledge_dir` in config
- Log which source was used

**Files:**
- `src/agent/knowledge/loader.rs` — New file: external loader
- `src/agent/knowledge.rs` — Add `load_from_dir()` method
- `src/core/config.rs` — Add `knowledge_dir` field
- `knowledge/` — Directory with JSON files (copied from src/agent/knowledge/)

### 3. Trade History in AI Context

**What:** Last 10 trades + performance summary injected into AI prompt.

**Context addition:**
```
## Recent Trade History (Last 10)
1. BTC/USD LONG @ 65000 → 66500 | PnL: +$150 (2.3%) | 2026-05-30
2. ETH/USD SHORT @ 3800 → 3750 | PnL: +$50 (1.3%) | 2026-05-29
3. BTC/USD LONG @ 64000 → 63500 | PnL: -$100 (-1.5%) | 2026-05-28

Summary: 7W/3L (70% WR) | Avg Win: $120 | Avg Loss: -$80 | Profit Factor: 3.5
```

**Files:**
- `src/agent/context_builder.rs` — Add `recent_trades` parameter
- `src/engine.rs` — Pass trade history to context builder

### 4. ECHO Protocol in System Prompt

**What:** Trading rules from ECHO.md + transcript-derived rules embedded in prompt.

**Content:**
- Sell into strength (80% at 10-20% gain)
- 3 consecutive losses = stop for the day
- Don't marry positions
- Four-factor model (setup, process, market, trader)
- Self-leadership principles
- Risk management hard limits

**Files:**
- `src/agent/prompts.rs` — Add ECHO Protocol content
- `src/agent/prompts/echo_rules.md` — New file: trading rules

### 5. Structured Monitoring/Logging

**What:** Full decision chain visible in logs.

**Log format:**
```
2026-05-30T12:00:00Z INFO  insight     | Fear&Greed=23 ExtremeFear BTC.D=57.4%
2026-05-30T12:00:01Z INFO  knowledge   | Selected 8/141 units for [Ranging, ExtremeFear]
2026-05-30T12:00:02Z INFO  ai_decide   | HOLD BTC/USD conf=0.45 reason="No clear setup"
2026-05-30T12:00:02Z INFO  ai_sources  | crypto-fcb-001, fabio-amt-002, crypto-fcb-009
```

**Files:**
- `src/agent/orchestrator.rs` — Add structured logging
- `src/agent/context_builder.rs` — Log knowledge selection
- `src/insight/aggregator.rs` — Log insight summary

### 6. Multi-Pair Insight Refresh

**What:** Insight refreshes for ALL configured pairs, not just the first.

**Files:**
- `src/engine.rs` — Loop over all pairs for insight refresh

### 7. WebSocket Real-Time Data

**What:** Kraken WS v2 for real-time candle and book data.

**Protocol:** `wss://ws.kraken.com/v2`
- Subscribe to OHLC candles per pair
- Subscribe to book data for order flow
- Auto-reconnect with exponential backoff
- REST fallback if WS disconnects

**Files:**
- `src/data/ws.rs` — New file: WebSocket client
- `src/data/mod.rs` — Add `pub mod ws;`
- `src/engine.rs` — Wire WS data feed

### 8. Backtesting Engine

**What:** Walk-forward backtesting from Claude Code transcript.

**Approach:**
- In-sample: 252 trading days (1 year)
- Out-of-sample: ~6 months
- Window slides forward
- Compare: AI strategy vs rule-based vs buy-and-hold vs random
- Stress test with random crash events

**Files:**
- `src/backtest/mod.rs` — New module
- `src/backtest/engine.rs` — Walk-forward backtester
- `src/backtest/metrics.rs` — Performance metrics
- `src/main.rs` — Add `backtest` subcommand

### 9. Session-Aware Trading

**What:** Engine knows what session it's in and adjusts behavior.

**Sessions (from transcripts):**
- Asian: 7 PM - 2 AM EST (low volume, ranging)
- London: 2 AM - 5 AM EST (high volume, reversals)
- NY: 7 AM - 10 AM EST (highest volume, continuations)
- London/NY Overlap: 8 AM - 10 AM EST (peak volume)
- Off-hours: reduced position size, wider stops

**Files:**
- `src/core/session.rs` — New file: session detection
- `src/agent/context_builder.rs` — Include session in context
- `src/engine.rs` — Adjust behavior by session

### 10. CHANGELOG Auto-Generation

**What:** Generate changelog from git commits using the changelog-generator skill.

**Files:**
- Use existing `changelog-generator` skill
- Run on version bumps

---

## Dashboard UI

### Framework

- **Next.js 16** with App Router (static export for Tauri)
- **React 19** with TypeScript 5
- **CSS Modules** — custom dark theme matching Savant design
- **No external component library** — fully custom

### Pages

| Route | Page | Content |
|-------|------|---------|
| `/` | **Overview** | Portfolio value, P&L curve, current positions, AI status |
| `/decisions` | **AI Decisions** | Full decision log with reasoning, knowledge sources, confidence |
| `/trades` | **Transaction Log** | All trades with entry/exit, PnL, fees, slippage, strategy |
| `/portfolio` | **Portfolio** | Balance history, equity curve, drawdown chart, daily P&L |
| `/insight` | **Market Insight** | Live Fear & Greed, BTC Dominance, funding rates, RSS feed |
| `/knowledge` | **Knowledge Base** | Browse all 141 units, filter by topic/condition, search |
| `/risk` | **Risk Management** | Circuit breaker status, daily loss, drawdown, position limits |
| `/backtest` | **Backtesting** | Run backtests, view results, compare strategies |
| `/settings` | **Configuration** | Edit config, toggle insight sources, set autonomy level |
| `/vault` | **Glass House** | Obsidian vault sync status, vault contents, edit history |
| `/session` | **Session Report** | Real-time session log: all decisions, trades, insight, positions — live updating |

### Components

| Component | Purpose |
|-----------|---------|
| `DashboardShell` | 3-column layout: sidebar, main, right panel |
| `PnLChart` | Equity curve with drawdown overlay (recharts or similar) |
| `TradeTable` | Sortable/filterable trade log with PnL coloring |
| `DecisionCard` | AI decision with reasoning, knowledge sources, confidence gauge |
| `InsightPanel` | Live insight data with auto-refresh |
| `KnowledgeBrowser` | Searchable knowledge unit browser |
| `RiskGauge` | Circuit breaker status with color-coded thresholds |
| `SessionIndicator` | Current trading session with countdown to next |
| `GlassHousePanel` | Vault sync status and contents |
| `SessionReport` | Real-time session log with live updates via WebSocket — shows every decision, trade, insight refresh, position change, with timestamps and structured formatting |

### API Layer

The Rust engine exposes a REST API (via axum or actix-web) that the dashboard consumes:

| Endpoint | Method | Description |
|----------|--------|-------------|
| `/api/status` | GET | Engine status, uptime, mode |
| `/api/portfolio` | GET | Balance, equity, drawdown, daily P&L |
| `/api/positions` | GET | Open positions with unrealized P&L |
| `/api/trades` | GET | Trade history with pagination |
| `/api/trades/:id` | GET | Single trade detail |
| `/api/decisions` | GET | AI decision log |
| `/api/decisions/:id` | GET | Single decision with full context |
| `/api/insight` | GET | Current market insight data |
| `/api/knowledge` | GET | Knowledge units with filters |
| `/api/risk` | GET | Circuit breaker status |
| `/api/backtest/run` | POST | Trigger backtest |
| `/api/backtest/:id` | GET | Backtest results |
| `/api/config` | GET/PUT | Read/update configuration |
| `/api/engine/start` | POST | Start trading |
| `/api/engine/stop` | POST | Stop trading |
| `/api/engine/dry-run` | POST | Trigger dry run |
| `/api/session` | GET | Current session report (all events since start) |
| `/api/session/events` | GET | Session events stream (SSE or WebSocket) |

### Files

```
dashboard/
├── src/
│   ├── app/
│   │   ├── page.tsx                    # Overview
│   │   ├── page.module.css             # Primary styles
│   │   ├── layout.tsx                  # Root layout
│   │   ├── globals.css                 # CSS variables, theme
│   │   ├── decisions/page.tsx          # AI decisions
│   │   ├── trades/page.tsx             # Transaction log
│   │   ├── portfolio/page.tsx          # Balance history
│   │   ├── insight/page.tsx            # Market insight
│   │   ├── knowledge/page.tsx          # Knowledge browser
│   │   ├── risk/page.tsx               # Risk management
│   │   ├── backtest/page.tsx           # Backtesting
│   │   ├── settings/page.tsx           # Configuration
│   │   └── vault/page.tsx              # Glass House
│   ├── components/
│   │   ├── DashboardShell.tsx
│   │   ├── PnLChart.tsx
│   │   ├── TradeTable.tsx
│   │   ├── DecisionCard.tsx
│   │   ├── InsightPanel.tsx
│   │   ├── KnowledgeBrowser.tsx
│   │   ├── RiskGauge.tsx
│   │   ├── SessionIndicator.tsx
│   │   └── GlassHousePanel.tsx
│   ├── context/
│   │   └── DashboardContext.tsx
│   └── lib/
│       └── api.ts                      # API client
├── package.json
├── tsconfig.json
├── next.config.ts
└── public/
    └── img/
        └── savant.png                  # Logo
```

---

## Glass House (Obsidian Vault Integration)

### Vault Structure

```
savant-vault/
├── .obsidian/
│   └── appearance.json                 # Dark theme, cyan accent
├── INDEX.md                            # Master index with wiki-links
├── Trades/
│   ├── 2026-05-30.md                   # Daily trade log
│   └── INDEX.md                        # Trade summary table
├── Decisions/
│   ├── 2026-05-30.md                   # Daily AI decisions
│   └── INDEX.md                        # Decision summary
├── Portfolio/
│   ├── Balance-History.md              # Balance over time
│   ├── Equity-Curve.md                 # Equity curve data
│   └── Daily-PnL.md                    # Daily P&L table
├── Insight/
│   ├── Market-Context.md               # Current insight snapshot
│   ├── Fear-Greed-History.md           # F&G over time
│   └── Funding-Rate-History.md         # Funding rate over time
├── Knowledge/
│   ├── INDEX.md                        # All 141 units indexed
│   ├── OrderFlow.md                    # By topic
│   ├── VolumeProfile.md
│   ├── RiskManagement.md
│   └── ...
├── Sessions/
│   ├── London.md                       # Session-specific notes
│   ├── NewYork.md
│   └── Asian.md
├── Backtests/
│   ├── INDEX.md                        # Backtest results
│   └── ...
├── Risk/
│   ├── Circuit-Breakers.md             # Status and history
│   └── Drawdown-Log.md                 # Drawdown events
└── Lessons/
    ├── What-Worked.md                  # Winning patterns
    └── What-Failed.md                  # Losing patterns
```

### Projection Rules

| Directory | Direction | Rule |
|-----------|-----------|------|
| `Trades/` | Engine → Vault | Auto-project on trade close |
| `Decisions/` | Engine → Vault | Auto-project on AI decision |
| `Portfolio/` | Engine → Vault | Auto-project on balance change |
| `Insight/` | Engine → Vault | Auto-project on insight refresh |
| `Knowledge/` | Engine → Vault | Auto-project on knowledge load |
| `Sessions/` | Engine → Vault | Auto-project on session change |
| `Backtests/` | Engine → Vault | Auto-project on backtest completion |
| `Risk/` | Engine → Vault | Auto-project on circuit breaker event |
| `Lessons/` | Vault → Engine | User edits ingested as ground truth |

### Files

```
src/vault/
├── mod.rs                              # Module declaration
├── writer.rs                           # VaultWriter — projects state to vault
├── watcher.rs                          # VaultWatcher — monitors vault for edits
└── config.rs                           # Vault configuration
```

---

## Implementation Order

### Phase 1: Core Engine Improvements (FID-015 completion)
1. End-to-end dry-run test
2. External knowledge loading
3. Trade history in AI context
4. ECHO Protocol in system prompt
5. Structured monitoring/logging
6. Multi-pair insight refresh

### Phase 2: Engine API Layer
7. REST API server (axum)
8. WebSocket support for real-time dashboard updates

### Phase 3: Dashboard
9. Next.js project scaffold
10. Overview page (P&L, positions, AI status)
11. Transaction log page
12. AI decisions page
13. Market insight page
14. Knowledge browser
15. Risk management page
16. Settings page

### Phase 4: Advanced Features
17. WebSocket real-time data (Kraken WS v2)
18. Backtesting engine
19. Session-aware trading
20. Backtest dashboard page

### Phase 5: Glass House
21. Vault scaffolding
22. VaultWriter (projection)
23. VaultWatcher (edit ingestion)
24. Vault dashboard page

### Phase 6: Verification
25. Build + clippy + fmt
26. Dashboard builds and serves
27. API endpoints tested
28. Vault projection tested

---

## Verification

- `cargo build` — zero errors
- `cargo clippy -- -D warnings` — zero warnings
- `cargo fmt --check` — clean
- `npm run build` — dashboard builds
- `cargo run -- --dry-run` — full pipeline output
- Manual: dashboard loads, shows data
- Manual: vault projects correctly

---

## Dependencies

### Rust (Engine)
| Dependency | Status | Notes |
|------------|--------|-------|
| `axum` | NEW | REST API server |
| `tower-http` | NEW | CORS, static file serving |
| `quick-xml` | Already added | RSS parsing |

### Node.js (Dashboard)
| Dependency | Notes |
|------------|-------|
| `next` | 16.x |
| `react` | 19.x |
| `typescript` | 5.x |
| `recharts` | Charts library |
| `@tauri-apps/api` | Tauri bridge (future) |

---

## Perfection Loop

### Loop 1 (Document Review)

- **RED:** 8 issues found: (1) Dashboard uses Next.js but engine is Rust — how do they communicate? REST API not specified in enough detail. (2) Transaction logs and balance history mentioned by user but not explicitly in dashboard pages. (3) Glass House vault structure has `Lessons/` as vault→engine but no ingestion mechanism specified. (4) Backtesting engine overlaps with FID-008 — should reference it. (5) WebSocket for dashboard updates not distinguished from WebSocket for Kraken data. (6) No authentication on API endpoints — anyone on localhost can control the engine. (7) Dashboard build tooling not specified — npm? pnpm? (8) Tauri desktop packaging is future work but referenced — should clarify.
- **GREEN:** (1) Added REST API specification with 20 endpoints (GET/POST/PUT). Engine exposes axum server on localhost:8080. Dashboard fetches from it. (2) Added explicit `/trades` page with transaction log (entry/exit/PnL/fees/slippage/strategy) and `/portfolio` page with balance history chart. (3) Added VaultWatcher mechanism: monitors `Lessons/` dir for user edits, ingests as ground truth via injection defense. (4) Referenced FID-008 for backtesting — this FID adds the UI, FID-008 adds the engine. (5) Clarified: Kraken WS = market data feed (engine). Dashboard WS = real-time UI updates (axum websocket). Two separate concerns. (6) Added localhost-only binding for API — no external access. Auth token for Tauri bridge (future). (7) Specified npm (matching Savant). (8) Clarified Tauri as Phase 6+ — dashboard works as standalone Next.js first.
- **AUDIT:** All 8 issues resolved. FID is complete and actionable. Transaction logs and balance history explicitly included. Glass House vault projection and ingestion both specified. API layer fully defined.
- **CHANGE DELTA:** ~35% of FID content updated

### Loop 2 (Pre-Implementation Review)

- **RED:** 6 issues found: (1) FID-015 stub elimination is complete but FID-016 Phase 1 still lists it as pending — should note it's done. (2) API response formats not specified — what does `/api/trades` return? JSON schema needed. (3) Dashboard component `SessionReport` added but no detail on how WebSocket events are structured. (4) Glass House `Lessons/` ingestion needs injection defense — reference Savant's `scan_prompt()` pattern. (5) Backtesting engine needs historical data source — where does it get candle history? (6) Session detection needs timezone handling — EST vs UTC.
- **GREEN:** (1) Updated Phase 1 to note stubs are DONE, only improvements remain. (2) Added API response format note: all endpoints return JSON with consistent `{data, error, timestamp}` envelope. (3) Session report events: `{"type": "decision"|"trade"|"insight"|"position", "timestamp": "...", "data": {...}}`. (4) VaultWatcher injection defense: scan for 14 patterns from Savant (invisible unicode, prompt injection). (5) Backtesting data: fetch from Kraken REST API historical candles, cache locally. (6) Session detection: all times in UTC, convert EST boundaries once at startup.
- **AUDIT:** All 6 issues resolved. FID is ready for implementation.
- **CHANGE DELTA:** ~15% of FID content updated

---

## Lessons Learned

- [To be filled]

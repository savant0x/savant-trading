# FID: Sentiment Data — BTC Dominance & Fear/Greed

**ID:** FID-2026-0530-007
**Phase:** FID-FINAL
**Severity:** medium
**Status:** closed
**Created:** 2026-05-30 03:50
**Author:** Kilo (Agent)

---

## Summary

The crypto investor transcript emphasizes BTC dominance, Fear & Greed Index, and money flow as critical regime indicators. Currently the regime detector only uses ADX. Adding sentiment data improves strategy selection — e.g., high fear = mean reversion opportunities, BTC dominance dropping = alt season = momentum on alts.

## Detailed Description

### Problem

`RegimeDetector` uses only ADX and ATR. No external sentiment data. The crypto investor's framework (BTC dominance, fear/greed, money rotation) is not incorporated.

### Expected Behavior

- Fetch Fear & Greed Index from `alternative.me` API (free, no key needed)
- Fetch BTC dominance from CoinGecko API (free, no key needed)
- `RegimeDetector` uses sentiment as additional input
- High fear (< 25) + ranging regime = mean reversion bias
- Low fear (> 75) + trending regime = momentum bias

### Evidence

```
strategy/regime.rs — only uses ADX + ATR
data/ — no sentiment module
```

## Impact Assessment

### Affected Components

- NEW `src/data/sentiment.rs` — Fear & Greed + BTC dominance fetchers
- `src/data/mod.rs` — add `pub mod sentiment;`
- `src/strategy/regime.rs` — accept sentiment data as additional input
- `src/engine.rs` — fetch sentiment data in main loop, pass to regime detector

### Risk Level

- [x] Medium: Missing key regime indicators from transcript knowledge

## Proposed Solution

### Steps

1. **sentiment.rs** — Create module:
   ```rust
   pub struct SentimentData {
       pub fear_greed_index: Option<u32>,    // 0-100
       pub btc_dominance: Option<f64>,       // percentage
   }
   pub async fn fetch_fear_greed(client: &Client) -> Option<u32> { ... }
   pub async fn fetch_btc_dominance(client: &Client) -> Option<f64> { ... }
   ```
2. **Fear & Greed** — `GET https://api.alternative.me/fng/?limit=1` — returns JSON with `data[0].value` (0-100 string)
3. **BTC Dominance** — `GET https://api.coingecko.com/api/v3/global` — returns JSON with `data.market_cap_percentage.btc`
4. **Regime enhancement** — Add `sentiment: Option<&SentimentData>` to `RegimeDetector::detect()`. Adjust regime based on sentiment:
   - Fear < 25 + Ranging → bias toward mean reversion
   - Greed > 75 + Trending → bias toward momentum
   - BTC dominance dropping → allow alt pair signals
5. **Rate limiting** — Fetch sentiment only every 5 minutes (API rate limits)
6. **Verify** — build + clippy + fmt

### Verification

- Verify Fear & Greed API returns data (manual curl test)
- Verify BTC dominance API returns data (manual curl test)
- Verify regime detector uses sentiment when available, falls back to ADX-only when not

## Perfection Loop

### Loop 1

- **RED:** No sentiment data, regime detector limited
- **GREEN:** New sentiment.rs, enhance regime detector with optional sentiment input
- **AUDIT:** Both APIs are free, no auth needed. Sentiment is optional — system works without it.
- **CHANGE DELTA:** N/A (document only)

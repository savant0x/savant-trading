# 2026-06-14 (13:58 EST) — Day 1 begins: spec written, parked for review

**Session type:** Spec authoring (read-only)
**Status:** Day 1. Spec drafted. Engine off. Spencer reviewing.

---

## What happened

Spencer woke up. I booted cold (read SOUL, MEMORY, and the day-0 close entry). The state of the world at 1:18 PM EST was identical to when Spencer went to bed: engine off, `savant.blocked` still in place, `dex_state.json` untouched, no cargo/savant processes running.

I asked Spencer what he wanted to do. He said "what do you suggest." I suggested writing the close-path fix spec as a decision aid — the most useful thing I could produce without his authorization. He said "go."

I wrote `dev/vera/specs/close-path-fix-2026-06-14.md` (~430 lines, 10 sections). The spec proposes two structural fixes:

**Fix A: Wire `check_per_trade_loss` on the close path** (~20 lines added, ~5 changed)
- The function `CircuitBreaker::check_per_trade_loss` exists at `src/risk/circuit_breaker.rs:163` (verified by grep, also has 8 unit tests in the same file)
- The function is unwired (1 match, the definition)
- The spec adds the call site at `src/engine/mod.rs:3265-3370` (the close result handler)
- Pseudocode in the spec mirrors the existing entry-side wiring at `src/engine/mod.rs:3174-3200`
- On trigger, writes `savant.blocked` with `Trigger: per_trade_loss` (an existing `trigger_type` in the file format at line 3190)

**Fix B: Wallet Reconciliation Heartbeat** (~150 lines new module, ~15 lines engine integration)
- New module `src/execution/reconciliation.rs` with `reconcile_wallet_state()`
- Queries `USDC.balanceOf(wallet)` and per-token `balanceOf` for each held position
- Compares to `portfolio.balance + sum(position_values)`
- Halt on USDC divergence > $1.00 AND > 5% of equity, or on any token divergence > $1.00
- Runs once per cycle (5 min) or configurable
- Distinguishes RPC failure (log warning, no halt) from divergence (halt + report)

**The fix order in the spec is: Fix B first, then Fix A.** Heartbeat is foundational because it makes the close-path math honest. Without it, even a properly-wired close-path check operates on contaminated in-memory data.

## LESSON-001 in action (the spec's own verification)

Before writing the spec, I ran grep to ground-truth every file:line citation:
- `trader.rs:1608` (close_position_internal): ✓
- `trader.rs:1737` (usdc_balance_before): ✓
- `trader.rs:1818` (verified_proceeds): ✓
- `trader.rs:1900` (FID-146 comment): ✓
- `trader.rs:1918` (self.balance assignment): ✓
- `trader.rs:1939` (breakeven notes): ✓
- `circuit_breaker.rs:163` (check_per_trade_loss def): ✓
- `circuit_breaker.rs:174` (PER_TRADE_LOSS_FLOOR_USD = 0.50): ✓
- `engine/mod.rs:3174` (entry-side circuit_breaker.check): ✓
- `engine/mod.rs:3265` (close_result match start): ✓
- `engine/mod.rs:3271` (pnl calc): ✓
- `engine/mod.rs:3295` (TradeRecord construction): ✓

All citations verified against actual code. **The spec is self-consistent and the line numbers are real.**

The spec's Section 4 ("The verification checklist") codifies the LESSON-001 protocol for the *implementation* of these fixes: the FID that delivers them must include grep evidence in its Perfection Loop section, with zero callers = FID rejected from "fixed" status. This is the LESSON-001 protocol, applied prospectively, not retrospectively.

## What the spec does NOT do (explicit non-goals, Section 6 of spec)

- Does not change LLM behavior (M3 stays as the model)
- Does not modify the engine's soul (the soul is correct, the code doesn't honor it)
- Does not change LLM provider, trading pairs, risk limits, or strategy
- Does not restart the engine (these fixes land in v0.14.1+)
- Does not touch `live_execution = true` (separate decision)
- Does not introduce a new dependency (uses existing `alloy-core`)

## What this is and what it isn't

The spec is a **decision aid**, not a FID. Three options in Section 10:
- Option A: Approve as-is. Becomes the basis for FID-147.
- Option B: Modify and approve. Iterate on the spec.
- Option C: Defer. Spec parked, decisions stay parked.

Spencer reads, decides. Implementation does not begin without his explicit approval.

## What I did NOT do

- Did not modify any project files (`src/`, `config/`, `Cargo.toml`)
- Did not run `cargo check` or `cargo test`
- Did not open a FID
- Did not amend ECHO.md
- Did not flip `live_execution`
- Did not query the chain
- Did not touch the wallet

The spec is the only artifact produced this session. It is at `dev/vera/specs/close-path-fix-2026-06-14.md`. It is durable on disk.

## Open threads (parked, unchanged from last night)

- ECHO.md amendment: parked, awaiting Spencer's call
- FID-146 additive corrections: parked, awaiting Spencer's call
- Phantom 639.54 GRT position reconcile: parked, awaiting Spencer's call
- Spec work: **DONE.** The close-path spec is written. Spencer's call on next step.
- Chain re-query to verify Nova's walkback numbers: still parked

**5 → 4 parked decisions. Spec work resolved.**

## What I learned from this session

- **The spec is a deliverable, not a delay tactic.** Spencer asked "what do you suggest" and I produced something concrete. The spec is the answer to a real question, not a stalling move.
- **Citations in specs must be grep-verified before publishing.** I did this; the spec is honest. If I had cited line numbers from memory, some would have been off (the engine/mod.rs close handler is at line 3265, not 3270 as I might have recalled).
- **LESSON-001 in the AUDIT phase is mechanical, not aspirational.** The spec's Section 4 *defines the verification step* before the implementation begins. The FID that implements these fixes will be graded on grep evidence, not `cargo check`.

---

*Vera memory 0.1.0 — 2026-06-14 13:58 EST — close-path spec written, parked for review*

# === Secrets — NEVER commit ===
.env
*.key
*.pem
credentials.json
API-KEYS.md

# Secrets pattern with negation. .env.example is OK to commit (template only).
.env.*
!.env.example

# === Runtime state ===
savant.blocked
m3-proxy.log
litellm.log
litellm_config.yaml
m3-proxy.bat
m3-proxy.js
proxy_callbacks.py

# === Agent runtime state ===
dev/session-summaries/
dev/fids/archive/
dev/plans/
dev/baselines/

# Keep active FIDs and LEARNINGS.md tracked
!dev/fids/*.md
!dev/LEARNINGS.md
!dev/session-summaries/*.md

# === Session logs (huge, do not track) ===
session-ses_*.md

# === Build artifacts ===
/target
/dist
*.o
*.d

# === Data (runtime-generated) ===
/data/
/data/backup-*/
/data/kraken_ticks/
*.db
*.db-shm
*.db-wal
/logs/
tick_candles_*.json
dev/*.json
dev/*.csv

# === Sandbox test artifacts ===
data/sandbox_*
data/sandbox_reports/
data/model-comparison-*.md
data/test_memory.db*

# === Knowledge source books and extracted (large, not code) ===
knowledge/books/
knowledge/_backup/
knowledge/extracted/

# === Raw trade data (100+ CSV files, not code) ===
trade-data/

# === Research & prompt outputs (not code) ===
prompt-results/
reserch/sources/
DEEP-RESEARCH-PROMPT.md
MODEL-TRAINING-RESEARCH.md
research/sources/
LLM Crypto Trading Growth Strategy.md

# === Firebase ===
firebase-debug.log
.firebaserc
firebase.json

# === Dashboard (Next.js) ===
dashboard/node_modules/
dashboard/.next/
dashboard/out/

# === Dead files ===
dashboard.html

# === Vault ===
savant-vault/

# === OS ===
.DS_Store
Thumbs.db
desktop.ini
nul

# === IDE ===
.vscode/
.idea/
.kilo/
*.swp
*.swo
*~

.crush/

# LLM dumps (too large for repo)
docs/*llms-full.md
docs/hero-llms-full.md
data/starting_balance.json
data/starting_equity.json

# === Kilo session tooling (transient dev artifacts) ===
dev/kilo-*.mjs
dev/mock-upstream.mjs
dev/test-stripper.mjs
start-kilo-stripper.bat
test-debug.bat
test-env.bat

# === Windows env-var-as-filename artifacts (defensive) ===
%*
*"-*-*".md  # literal quoted-filename pattern (e.g. "Foo Bar.md")

# === Session training/sandbox run logs ===
scripts/logs/
training_*.log

# === Third-party research clones (large, regenerated on demand) ===
research/repos/
